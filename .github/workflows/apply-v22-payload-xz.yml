name: Apply V2.2 payload XZ

on:
  push:
    branches: ["feat/freedom-book-v2-2-production"]
    paths: [".v22-payload.b64"]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Expand V2.2 payload
        shell: bash
        run: |
          set -euo pipefail
          base64 -d .v22-payload.b64 > /tmp/v22.tar.xz
          tar -xJf /tmp/v22.tar.xz -C .
          rm -f .v22-payload.b64
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: publish Freedom Book V2.2 production candidate"
          git push origin HEAD:feat/freedom-book-v2-2-production
