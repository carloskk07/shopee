# Produção

Branch publicada: `main`.

Domínio: `achadostube.com.br`.

Fluxo esperado:

`branch -> Pull Request -> Site integrity / validate -> squash merge -> main`

Nunca usar force push na `main` como fluxo normal de publicação.
